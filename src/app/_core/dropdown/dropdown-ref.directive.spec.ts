import {DropdownRefDirective} from './dropdown-ref.directive';

describe('DropDownRefDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownRefDirective();
    expect(directive).toBeTruthy();
  });
});
