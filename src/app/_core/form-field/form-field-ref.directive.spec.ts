import {FormFieldRefDirective} from './form-field-ref.directive';

describe('FormFieldRefDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldRefDirective();
    expect(directive).toBeTruthy();
  });
});
